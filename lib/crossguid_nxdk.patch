diff --git a/CMakeLists.txt b/CMakeLists.txt
index 76b5a62..9c80bdc 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -29,6 +29,8 @@ elseif(APPLE)
 elseif(ANDROID)
     # GUID_ANDROID is used in the headers, so make PUBLIC
     target_compile_definitions(crossguid PUBLIC GUID_ANDROID)
+elseif(NXDK)
+		target_compile_definitions(crossguid PRIVATE GUID_NXDK)
 else()
     find_package(Libuuid REQUIRED)
     if (NOT LIBUUID_FOUND)
diff --git a/src/guid.cpp b/src/guid.cpp
index 3061a9b..7a5e3b0 100644
--- a/src/guid.cpp
+++ b/src/guid.cpp
@@ -388,6 +388,30 @@ Guid newGuid()
 }
 #endif
 
+#ifdef GUID_NXDK
+Guid newGuid()
+{
+	// based on https://github.com/xbmc/xbmc/blob/Isengard/xbmc/utils/StringUtils.cpp#L1074
+	static bool m_uuidInitialized = false;
+	if (!m_uuidInitialized)
+	{
+		srand(time(NULL));
+		m_uuidInitialized = true;
+	}
+
+	std::array<unsigned char, 16> bytes;
+	for (int i = 0; i < 16; i++)
+	{
+		bytes[i] = static_cast<unsigned char>(rand() % 256);
+	}
+
+	// Set UUID version (4) and variant (RFC 4122 compliant)
+	bytes[6] = (bytes[6] & 0x0F) | 0x40; // Version 4 (random)
+	bytes[8] = (bytes[8] & 0x3F) | 0x80; // Variant (RFC 4122)
+
+	return Guid{std::move(bytes)};
+}
+#endif
 
 END_XG_NAMESPACE
 
